import { ApexEnterprisePage } from "../ApexPortalPages";

export const metadata = {
  title: "Enterprise | APEX Education OS",
  description: "Multi-branch schools and large networks — custom APEX deployment, integrations and dedicated support.",
};

export default function Page() {
  return <ApexEnterprisePage />;
}
